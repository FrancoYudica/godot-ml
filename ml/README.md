1. Create the virtual environment and activate it
2. Install the requirements, specified on `requirements.txt`
3. Register the venv as a jupiter kernel: `python -m ipykernel install --user --name=ml-venv --display-name "ML (venv)"`



Great site for checking the ONNX models: [netron](https://netron.app/)